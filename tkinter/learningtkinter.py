# from tkinter import *

# window = Tk()

# labelWidget = Label(window, text="Hello world")
# labelWidgetGrid = Label(window, text="Amongus")


# labelWidgetGrid.grid(row=0, column=0)
# labelWidget.grid(
#     row=1, column=0
# )  # pack() adds the widget to the window, can't use grid and pack at same time

# window.mainloop()

amongus = 5
match amongus:
    case 1:
        print(1)
    case 5:
        print(5)

